{
  "name": "getbus-api",
  "version": "1.1.0",
  "description": "Projeto do exercício da rodoviária - Raro Academy",
  "type": "module",
  "scripts": {
    "build": "babel src -d dist",
    "start": "npm run build && node dist/shared/index.js",
    "dev": "npm run build && node --watch dist/shared/index.js",
    "test:dev": "node --test --watch test/",
    "test": "node --test test/"
  },
  "devDependencies": {
    "@babel/cli": "^7.21.5",
    "@babel/core": "^7.22.1",
    "@babel/plugin-proposal-decorators": "^7.22.3",
    "@babel/preset-env": "^7.22.4"
  }
}
